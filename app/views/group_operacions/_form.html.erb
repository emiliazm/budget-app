<%= form_with(model: group_operacion) do |form| %>
  <% if group_operacion.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(group_operacion.errors.count, "error") %> prohibited this group_operacion from being saved:</h2>

      <ul>
        <% group_operacion.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :group_id, style: "display: block" %>
    <%= form.text_field :group_id %>
  </div>

  <div>
    <%= form.label :operation_id, style: "display: block" %>
    <%= form.text_field :operation_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
